# codegraph

Multi-language workspace code graph orchestrator. Auto-discovers services,
frontends, gateways, and libraries in a workspace, orchestrates builds using
language-specific tools (gograph, tsgraph, pygraph), and exposes a unified
MCP server for querying any symbol across any service.

## Status

Early development. See TODOS.md for progress.
